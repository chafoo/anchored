---
name: build
description: Build-Stage. /a:build <slug>. Fraktaler build — each-Loop ueber Kinder (interleaved) bzw. Leaf-Arbeit (implement + validate). stop + retry_limit greifen.
---

# /a:build

TODO. Loopt Kinder (each) oder fuehrt am Leaf implement + task-/code-validate aus. Via anchored-CLI.
