{
  "name": "anchored",
  "owner": {
    "name": "chafoo",
    "url": "https://github.com/chafoo"
  },
  "metadata": {
    "description": "anchored — evidence-driven development for Claude Code. Fractal task lifecycle (plan→refine→build→wrap) with step-enforcement receipts, backed by the `anchored` CLI."
  },
  "plugins": [
    {
      "name": "a",
      "source": "./plugin",
      "description": "Fractal task lifecycle — plan→refine→build→wrap on every tier (phase/task/epic). CLI-only transport via the `anchored` binary over Bash, no MCP. Slash commands: /a:plan /a:refine /a:build /a:wrap."
    }
  ]
}
