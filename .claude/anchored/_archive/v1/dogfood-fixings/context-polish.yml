# yaml-language-server: $schema=https://raw.githubusercontent.com/chafoo/anchored/main/plugin/references/schema/task.schema.json
schema_version: 2
slug: dogfood-fixings/context-polish
title: Polish — context fields, created, list-phases, trigger quirk (F2 + F3 + F4 + F6)
status: done
created: 2026-06-11
context:
  plan: |
    The low-priority findings bundled together:
    F2: context.plan/refine/build stay empty (everything goes into the log). DECISION
    (q3): populate context.* via set-field (dotted-path) like v1; log[] stays the
    PM notebook.
    F3: created field not set on create. DECISION (q6): set it via an injected
    clock seam (the bin supplies the time; no Date.now in the engine code).
    F4: `node list-phases` does not exist — the skill fell back to read.
    F6: `/a:refine` briefly showed "Unknown command" (trigger quirk, not blocking).
    REF: .claude/temp/dogfood-findings.md F2/F3/F4/F6.
questions: []
log: []
phases:
  - name: populate context trails (F2)
    slug: context-fill
    status: done
    context: |
      plan/refine/build agents + skills write their prose trails to
      context.plan/refine/build via set-field (dotted-path). wrap-review/summarize
      read context as a trail. log[] stays the append-only notebook.
    rules:
      - path: .claude/rules/cli-only-transport.md
        why: context via set-field through the CLI
    acceptance_criteria:
      - id: a1
        text: "After plan + refine, context.plan + context.refine are populated (set-field dotted-path), not empty. Evidence: a run/test → node read shows context.plan/refine as a prose trail (file:line in the skills/agents)."
        status: done
        evidence:
          - plugin/skills/plan/SKILL.md + refine/SKILL.md write context.plan/context.refine via set-field (dotted-path) at the stage end; log[] stays the PM notebook (q3)
  - name: created field via clock seam (F3)
    slug: created-field
    status: done
    context: |
      create sets `created` (ISO date) via an injected now() seam; the bin
      supplies the real time. No Date.now in the engine/core code.
    rules:
      - path: .claude/rules/factory-functions.md
        why: now() as an injected seam, fakeable; no top-level effect
    acceptance_criteria:
      - id: a1
        text: "create sets a created field via an injected now() dep; the bin wires the real time, tests fake it. Evidence: facade/create test (created set) + file:line bin (now wiring) (green)."
        status: done
        evidence:
          - core/src/ops/facade.ts create stamps created via the injected now() seam; core/src/bin.ts now=new Date().toISOString().slice(0,10); epic.ts created field added; epic-tier.spec created test green; CLI smoke created:2026-06-11
  - name: list-phases + trigger quirk (F4 + F6)
    slug: polish-rest
    status: done
    context: |
      F4: either add `node list-phases <slug>` as a convenience verb, OR officially
      settle the skill/agent docs on `node read` + phases[].
      F6: observe the `/a:refine` "Unknown command" quirk on the next run;
      if needed, check the plugin name `a` (single letter) as the cause.
    rules:
      - path: .claude/rules/cli-only-transport.md
        why: consistent verb surface / clear convention
    acceptance_criteria:
      - id: a1
        text: "F4 decided + implemented: either `node list-phases` exists (CLI smoke ok), OR the docs settle on read+phases[] (file:line). "
        status: done
        evidence:
          - core/src/cli/commands/node.ts list-phases verb (read+phases extract); epic-tier.spec list-phases test green; CLI smoke list-phases ok
      - id: a2
        text: "F6 clarified: the refine trigger quirk is classified (observed/root cause) + fixed if needed (e.g. plugin name). Evidence: note/proof."
        status: done
        evidence:
          - 'F6 refine trigger quirk classified: "Unknown command: /a:refine" was a transient display artefact BEFORE the skill run (the skill then ran through normally afterwards); not blocking, likely plugin reload timing/single-letter name. Low-impact, observed.'
