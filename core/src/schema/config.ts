/**
 * anchored.yml-Schema (Zod). Tiers (phase/task/epic/project), je Stages mit steps;
 * _lib (YAML-Anchors erlaubt auf DIESEM Pfad), _fields/custom fields. Top-level strict.
 * TODO(pure-engine).
 */
export {}
