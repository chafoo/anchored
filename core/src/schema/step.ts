/**
 * Step-Grammatik (Zod, strukturell): name + (run XOR use+type) + optional instructions;
 * involve nur auf walk (all|high-only|none); each + optional steps-Body auf loop.
 * Reserved-Name-Semantik NICHT hier -> resolve-steps.
 * TODO(pure-engine).
 */
export {}
