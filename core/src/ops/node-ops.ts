/**
 * createNodeOps(tierSchema, deps) -> tier-generischer Op-Kern.
 * create/read/set-status/add-child/set-child-status/move-child/next-child/
 * add-question/resolve-question/append-log/set-field/add-evidence.
 * read -> validate -> mutate -> re-validate -> atomicWrite. tierSchema parametrisiert.
 * TODO(pure-engine).
 */
export {}
