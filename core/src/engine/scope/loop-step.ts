/**
 * each: -> pro Kind den Body (steps) INTERLEAVED durchfahren (stepRunner),
 * dann advance + stop-check; ruft createTierRunner(child) fuer die Kind-Lifecycle.
 * Hier schliesst sich die Rekursion. Kurzform `each` ohne Body = impliziter [run].
 * TODO(pure-engine / epic-tier).
 */
export {}
